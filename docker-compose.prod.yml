services:
  pipeline:
    environment:
      ENVIRONMENT: production
    volumes:
      # Keep only persistent data lake volume in production-like runs.
      - ./data:/app/data

  postgres:
    ports: []
